import "../css/style.scss"
// Import the main SCSS stylesheet.

// Our modules / classes
import MobileMenu from "./modules/MobileMenu"
import HeroSlider from "./modules/HeroSlider"
import GoogleMap from "./modules/GoogleMap"
import Search from "./modules/Search"
import MyNotes from "./modules/MyNotes"
import Like from "./modules/Like"
// Import the JavaScript modules/classes for various features.

// Instantiate a new object using our modules/classes
const mobileMenu = new MobileMenu()
// Create a new instance of the MobileMenu class.

const heroSlider = new HeroSlider()
// Create a new instance of the HeroSlider class.

const googleMap = new GoogleMap()
// Create a new instance of the GoogleMap class.

const search = new Search()
// Create a new instance of the Search class.

const myNotes = new MyNotes()
// Create a new instance of the MyNotes class.

const like = new Like()
// Create a new instance of the Like class.
